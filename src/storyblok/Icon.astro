---
import { storyblokEditable } from "@storyblok/astro";
import { Icon as AstroIcon } from "astro-icon";

const { blok } = Astro.props;
---

<AstroIcon {...storyblokEditable(blok)} class="h-8 md:h-12" name={blok.name} />
