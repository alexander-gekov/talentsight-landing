---
import { storyblokEditable } from '@storyblok/astro'
import { Icon } from "astro-icon";
 
 const { blok } = Astro.props
 ---
  
 <Icon {...storyblokEditable(blok)} class="h-8 md:h-12" name={blok.name} />